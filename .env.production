# Production Environment Variables for Render
NODE_ENV=production
PORT=10000
MONGODB_URI=mongodb+srv://your-username:your-password@your-cluster.mongodb.net/lca_database?retryWrites=true&w=majority
JWT_SECRET=your_super_secure_jwt_secret_key_minimum_32_characters_long

# Optional: Add these if you plan to use AI features
# OPENAI_API_KEY=your_openai_api_key_here

# Instructions:
# 1. Replace MONGODB_URI with your MongoDB Atlas connection string
# 2. Generate a secure JWT_SECRET using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# 3. Copy and paste this file content into Render's "Add from .env" feature
# 4. DO NOT commit this file to Git (it's already in .gitignore)