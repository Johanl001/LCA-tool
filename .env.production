# Production Environment Variables for Render
NODE_ENV=production
PORT=10000

# MongoDB Atlas Connection (replace with your actual Atlas connection string)
# Migration from MongoDB Compass: Export local data and import to Atlas
MONGODB_URI=mongodb+srv://your-username:your-password@cluster0.xxxxx.mongodb.net/lca_database?retryWrites=true&w=majority

# JWT Secret (generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
JWT_SECRET=your_super_secure_jwt_secret_key_minimum_32_characters_long

# OpenRouter Configuration for AI Assistant
OPENROUTER_API_KEY=your_openrouter_api_key_here
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
OPENROUTER_MODEL=openai/gpt-oss-20b:free
OPENROUTER_APP_NAME=LCA_TOOL
OPENROUTER_SITE_URL=https://your-app-name.onrender.com

# Instructions for MongoDB Compass Users:
# 1. Create MongoDB Atlas account (free tier available)
# 2. Export your local data: mongoexport --host localhost:27017 --db lca_database --collection projects --out projects.json
# 3. Import to Atlas: mongoimport --uri "your-atlas-uri" --collection projects --file projects.json
# 4. Replace MONGODB_URI above with your Atlas connection string
# 5. Generate JWT_SECRET and add your OpenRouter API key
# 6. Copy this content to Render's "Add from .env" feature