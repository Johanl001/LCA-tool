# Production Environment Variables for Render
# Copy and paste this entire content into Render's "Add from .env" feature

NODE_ENV=production
PORT=10000
MONGODB_URI=mongodb+srv://your-username:your-password@your-cluster.mongodb.net/lca_database?retryWrites=true&w=majority
JWT_SECRET=your_super_secure_jwt_secret_key_minimum_64_characters_long

# OpenRouter Configuration for gpt-oss-20b model
OPENROUTER_API_KEY=sk-or-v1-your-actual-openrouter-api-key-here
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
OPENROUTER_MODEL=openai/gpt-oss-20b:free
OPENROUTER_APP_NAME=LCA_TOOL
OPENROUTER_SITE_URL=https://your-app-name.onrender.com

# IMPORTANT INSTRUCTIONS:
# 1. Replace all "your-*" placeholders with actual values
# 2. Get MongoDB URI from MongoDB Atlas
# 3. Generate JWT secret: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# 4. Use your actual OpenRouter API key (starts with sk-or-v1-)
# 5. Replace "your-app-name" with your actual Render app name